package over.achievers.database.validation;

import over.achievers.database.DatabaseEntry;

public class EmailValidator implements Validator{

    @Override
    public boolean isValid(DatabaseEntry entry) {

    }
}
